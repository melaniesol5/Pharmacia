{% extends 'base.html.twig' %}

{% block body %}
    <h1>Analises list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Hemograma</th>
                <th>Rayosx</th>
                <th>Testpsicologico</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for analisi in analises %}
            <tr>
                <td><a href="{{ path('analisis_show', { 'id': analisi.id }) }}">{{ analisi.id }}</a></td>
                <td>{{ analisi.name }}</td>
                <td>{{ analisi.hemograma }}</td>
                <td>{{ analisi.rayosX }}</td>
                <td>{{ analisi.testPsicologico }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('analisis_show', { 'id': analisi.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('analisis_edit', { 'id': analisi.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('analisis_new') }}">Create a new analisis</a>
        </li>
    </ul>
{% endblock %}
